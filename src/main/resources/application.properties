spring.application.name=ticket-tracker
server.port=8080

# ── Active profile: "h2" for local dev, "mysql" for production ──────────────
spring.profiles.active=h2

# ── JPA common settings ──────────────────────────────────────────────────────
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
